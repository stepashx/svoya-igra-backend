/**
 * Commerce schema — placeholder (Stage 5A.1).
 *
 * Tables (shop items, QR tools, purchases) are NOT defined yet. Decisions to
 * honour when they do:
 *   - `shop_items` is a global catalog — no purchase-state fields on it.
 *   - `qr_tools` is a global catalog.
 *   - Purchase state lives in `purchases`.
 */
export {};
