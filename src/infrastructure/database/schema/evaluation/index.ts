/**
 * Evaluation schema — placeholder (Stage 5A.1).
 *
 * Tables (evaluation criteria, evaluations/scores) are NOT defined yet.
 * Evaluator-type vocabulary lives in `../shared/enums/evaluator-type.enum`;
 * reuse it, do not redeclare.
 */
export {};
