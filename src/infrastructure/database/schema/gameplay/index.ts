/**
 * Gameplay schema — placeholder (Stage 5A.1).
 *
 * Tables (categories, questions, board cells, answers) are NOT defined yet.
 * Board cell state vocabulary lives in `../shared/enums/board-cell-state.enum`;
 * reuse it, do not redeclare.
 */
export {};
