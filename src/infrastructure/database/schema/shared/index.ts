/**
 * Cross-cutting schema building blocks: the shared enum/status vocabulary and
 * reusable column conventions. Imported by feature-area schema files so status
 * sets and id/timestamp semantics are defined once.
 */
export * from './enums';
export * from './columns';
