import { Module } from '@nestjs/common';

/**
 * Gameplay feature area — placeholder shell only (Stage 3A).
 *
 * Later: Game Flow coordinator, 6x5 board, questions and host-only answer
 * reveal, scoring (sole owner of earnedScore/balance), turns, and timers.
 * No logic exists yet.
 */
@Module({})
export class GameplayModule {}
