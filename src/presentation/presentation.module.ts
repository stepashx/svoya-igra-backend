import { Module } from '@nestjs/common';

/**
 * Presentation feature area — placeholder shell only (Stage 3A).
 *
 * Later: requirements, preparation timer, captain upload to MinIO with DB
 * metadata, late penalty, file visibility via publicUrl, and thin defense
 * ordering. No logic exists yet.
 */
@Module({})
export class PresentationModule {}
